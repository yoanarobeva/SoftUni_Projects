function lettersCombinations (input) {
    let start = input[0];
    let end = input[1];
    let letter = input[2];

    let alphabet = "abcdefghijklmnopqrstuvwxyz";
    let counter = 0;

    for (let a = 0; a < alphabet.length; a++) {
        for (let b = 0; b < alphabet.length; b++) {
            for (let c = 0; c < alphabet.length; c++) {
                let first = alphabet.charAt(a);
                
            }
        }
    }
}