function sumFirstAndLastArrayElements (array) {
    let firstNumber = array[0];
    let lastumber = array[array.length - 1];

    console.log(firstNumber + lastumber);
}

sumFirstAndLastArrayElements([20, 30, 40])